import { PartnerDashboard } from '../components/partner-dashboard';

// Corps de la page /partner-console. Monté par la route générée (P9).
export default function DashboardPage() {
  return <PartnerDashboard />;
}
