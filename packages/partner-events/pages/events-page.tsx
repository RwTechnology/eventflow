import { EventsList } from '../components/events-list';

// Corps de la page /partner-console/evenements. Monté par la route générée (P9).
export default function EventsPage() {
  return <EventsList />;
}
