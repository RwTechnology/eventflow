import { PartnersList } from '../components/partners-list';

// Corps de la page /partenaires. Monté par la route générée dans l'app (P9).
export default function PartnersPage() {
  return <PartnersList />;
}
