import { ModerationQueue } from '../components/moderation-queue';

// Corps de la page /moderation. Monté par la route générée (P9).
export default function ModerationPage() {
  return <ModerationQueue />;
}
