// MODULE_MARKER: {{camel id}}_BUNDLE_MARKER
// Corps de page — vit UNIQUEMENT ici, dans le package (P9).
export function {{pascal id}}Page() {
  return (
    <div className="ef-page">
      <h1 className="ef-page-title">{{label}}</h1>
      <p>Module {{ns}}/{{id}}.</p>
    </div>
  );
}
