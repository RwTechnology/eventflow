import { buildAuthOptions } from '{{ns}}/auth';

// auth-options.ts (serveur) — compose NextAuth depuis les descripteurs des
// seuls modules embarqués (P12). Ajouter ici le <camel>AuthDescriptor de
// chaque module à backend embarqué par l'app.
export const authOptions = buildAuthOptions([]);
