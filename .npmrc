# Le scope @rwtechnology est servi par GitHub Packages (design system privé).
# Auth via la variable d'env GITHUB_TOKEN (scope read:packages). Jamais de token en clair.
@rwtechnology:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${GITHUB_TOKEN}
